services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-service
    ports:
      - "5173:5173"
    environment:
      - VITE_IDENTITY_SERVICE_URL=http://localhost:8000
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
